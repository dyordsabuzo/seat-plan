export enum BoatPosition {
    SCRATCH,
    RESERVE,
    DRUMMER,
    LEFT,
    RIGHT,
    SWEEP
}

// export enum BoatSize {
//     SMALL = 5,
//     STANDARD = 10
// }

export enum BoatLabel {
    EMPTY_SEAT = "Empty Seat"
}

export const BoatSize = {
    SMALL: 5,
    STANDARD: 10
}