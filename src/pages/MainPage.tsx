import Tabs from "../refactor/Tabs";

const MainPage: React.FC = () => {
    return (
        <div className={`p-4`}>
            <Tabs categories={[]}/>
        </div>
    )
}

export default MainPage;